sudo apt-get update -y
sudo apt-get upgrade -y

#Download Docker
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
apt-cache policy docker-ce
sudo apt-get install -y docker-ce
sudo systemctl status docker
ctrl c
sudo usermod -aG docker ${USER} #This makes it so you don't have to use sudo
#Log out and back in

#Install Git
sudo apt-get install git-core

#Install a key so you don't have to type in your git password
ssh-keygen -t rsa -b 4096
cd /.ssh
nano id_rsa.pub
#Press Esc then $ to word wrap. Copy that into github ssh key
#eval $(ssh-agent -s)
#ssh-add ~/.ssh/id_rsa

sudo apt-get install keychain
nano $HOME/.profile
#Copy the 2 text lines below to the top of the profile docker
/usr/bin/keychain $HOME/.ssh/id_rsa
source $HOME/.keychain/$HOSTNAME-sh

#Log out and back in. Enter the password for the ssh key once and you're good to go for life

#Clone a repo
git cone git@github.com:<org>/<repository name>

#FTP
#Set to SFTP
#Host is just the IP
#Select Logon Type as keyfile
#User is the username
#Show location to key file
#